{
  "name": "progressive-json-demo",
  "private": true,
  "version": "0.0.1",
  "description": "Demo for @yoyo-org/progressive-json with client and server",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "install:all": "npm install --workspaces",
    "dev:client": "npm run dev --workspace=client",
    "dev:server": "npm run dev --workspace=server"
  }
}
